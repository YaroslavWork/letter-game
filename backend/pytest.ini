[pytest]
DJANGO_SETTINGS_MODULE = backend.settings
python_files = test_*.py *_test.py
python_classes = Test*
python_functions = test_*
addopts = 
    --verbose
    --tb=short
    --strict-markers
testpaths = tests
markers =
    django_db: marks tests as requiring database access (django_db marker from pytest-django)
